# Text Editor

## How to run locally

You need Node.js and npm installed — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
# Clone the repository
git clone <YOUR_GIT_URL>

# Navigate to the project directory
cd <YOUR_PROJECT_NAME>

# Install dependencies
npm i

# Start the development server
npm run dev
```

## Technologies

This project uses:

- Vite
- TypeScript
- React
- shadcn-ui
- Tailwind CSS

## Deploy

Build the app with `npm run build`, then deploy the `dist` folder to any static host (e.g. Vercel, Netlify).
